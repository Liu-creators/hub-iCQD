langchain 工具调用 和 llm function call 有什么区别？
langchain 工具调用结构
1 定义工具
2 定义模型
3 模型绑定工具
4 触发LLM提问
5 讲LLM返回的结果添加到信息列表（含工具调用的信息）
6 循环调用工具，将工具的结果添加到信息列表
7 第二次出发LLM，获得最终结果

llm function call 结构
1 定义个人信息
2 定义工具
3 定义工具描述的schema
4 定义工具名称本地函数映射
5 定义执行工具函数
6 发起LLM提问
7 如果检测到调用工具，将第一次返回内容+工具调用结果添加到信息列表
8 第二次发起LLM提问，获得最终结果。

从结构来看：
两种都要把第一次LLM提问返回内容+工具调用的结果添加到信息中，最终调用LLM得到答复。但是LangChain做了多层封装，是一个抽象的框架。
从便捷性看：
LangChain不需要定义工具的Jason结构，据说还统一了接口，一套代码适配多个模型，开发效率高。
从运行速度看：
LLM 函数调用：速度最快。总耗时几乎完全取决于模型推理时间 + 网络延迟。
LangChain 工具调用：速度更慢、Token 消耗激增。由于存在多层抽象，会产生显著的额外开销。
langchain 工具调用 的 速度是受到什么影响？
1 版本、2 工具数量、3执行方式、4重试次数、5是否使用缓存
提升手段
